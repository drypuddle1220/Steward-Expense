import React, { useState } from 'react'

export default function Expense_Input_Mode() {

    const Expense_Input_Model = ({ isOpen, onClose, onSubmit}) => {
        const [amount, setAmount] = useState('');
        const [category, setCategory] = useState('');

    }

  return (
    <div>Expense_Input_Mode</div>
  )
}
